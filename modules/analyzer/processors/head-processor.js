export class HeadProcessor {
  constructor(lineLimit = 2) {
    this.lineLimit = lineLimit;
  }

  process(data) {
    //take first ${lineLimit} textParts and try to work with them as with a header
  }

  getResult() {
    return []; //return real result
  }
}
