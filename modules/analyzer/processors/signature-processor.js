export class SignatureProcessor {
  constructor(lineLimit = 2) {
    this.lineLimit = lineLimit;
  }

  process() {
    //take last ${lineLimit} textParts and try to work with them as with a header
  }

  getResult() {
    return []; //return real result
  }
}
